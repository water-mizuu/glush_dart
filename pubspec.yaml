name: glush
description: A versatile parser toolkit for Dart - generates parsers, supports LL/LR and context-free grammars, scannerless, streamable push-based API.
version: 2.0.0
homepage: https://github.com/judofyr/glush

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies: { meta: ^1.18.2 }
dev_dependencies:
  crypto: ^3.0.0
  lints: ^3.0.0
  path: ^1.9.0
  test: ^1.24.0
  benchmark_harness: ^2.2.0
